# ==================== 共享配置（所有环境） ====================
server:
  port: 8080

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
  configuration:
    map-underscore-to-camel-case: true
    #log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

sa-token:
  token-name: token
  timeout: 2592000
  active-timeout: -1
  is-concurrent: true
  is-share: false
  token-style: simple-uuid
  is-log: false

app:
  upload-path: ${UPLOAD_PATH:${user.dir}/uploads}
  allowed-origins: http://localhost:5173
  view-flush-interval: 30000

wechat:
  mini:
    appId: ${WX_APP_ID:wx79150c40bdf8c66b}
    appSecret: ${WX_APP_SECRET:f2e79e3dc4c8c61812405fae86bd39af}
